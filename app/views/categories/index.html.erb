<h1>カテゴリー一覧</h1>
<% @categories.each do |category| %>
  <span class="badge badge-info mr-1 p-2">
    <%= link_to category.name, category_path(id: category.id), method: :delete, class: "text-white"  %>
  </span>
<% end %>
<p><%= link_to 'カテゴリー作成', new_category_path %></p>
