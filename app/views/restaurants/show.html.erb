<% provide(:title, @restaurant.name)  %>
<%= render "restaurant_details", restaurant: @restaurant %>
<div class="row">
  <%=link_to "このお店の口コミを投稿する", new_restaurant_review_path(restaurant_id: @restaurant.id),class:"btn btn-primary"%>
  <h2><%= @restaurant.name %>の口コミ一覧</h2>
  <%= render "layouts/review_index", reviews: @reviews %>
</div>
<%= paginate @reviews %>
